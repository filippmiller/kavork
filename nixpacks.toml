[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "postgresql"]

[phases.install]
cmds = ["cd site_demo && python -m pip install -r requirements.txt"]

[start]
cmd = "cd site_demo && python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
